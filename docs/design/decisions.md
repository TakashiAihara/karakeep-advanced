# 設計上の決定記録

`extensions/tab-group` の roadmap を組むにあたって決めたことと、その理由。
決定は上から時系列。覆すときは行を書き換えず、新しい決定を下に足して「D0N を覆す」と書く。

判断の材料になった調査は `docs/design/roadmap.md` と、各 issue の「背景」節にある。

---

## D01 — restore したグループは既定で残し、消すかどうかを選べるようにする

決定日: 2026-08-28 / 状態: 採用

OneTab は「復元 = そのグループを消費する」が既定で、復元したタブはリストから消える。
現実装の `openGroup` は読むだけで list を消さないため、使い続けると sub-list が単調増加する。

検討した案:

- (a) OneTab 準拠で Open all したら削除する
- (b) 現状維持で常に残す
- (c) options で既定を決め、行ごとに「開くだけ / 開いて消す」を出す

**(c) を採用。**

Karakeep をバックエンドに置いた動機は「機種変やプロファイル破損でタブが消えるのを止める」ことで、
これはアーカイブとしての要求。一方 OneTab の日常運用は「ウィンドウを一旦退避して、後で消費する」で、
こちらは一時退避としての要求。**この 2 つは別物で、どちらも実際に要る。**
(a) はアーカイブ用途を壊し、(b) は一時退避用途で片付けが進まない。

既定は「残す」にする。破壊的でない側を既定にし、消す操作は明示的に選ばせる。

## D02 — ツールバーアイコンは popup のままとし、ドキュメント側を直す

決定日: 2026-08-28 / 状態: 採用

`docs/karakeep-advanced.md` の Phase 1 表は
「ツールバーアイコン 1 クリックで全タブ保存 + 全タブ閉じる (OneTab デフォルト挙動を踏襲)」と書いているが、
実装はアイコンクリックで popup が開き、Save を押す 2 アクションになっている。

**実装を正とし、ドキュメントを直す。**

理由は 2 つ。

- 即保存はショートカット (`Ctrl+Shift+E` = `save-tab-group`) が既に担っており、動線として失われていない
- アイコンから popup が開かないと、Recent / Search / Import に到達する手段が消える。
  ツールバーアイコンは拡張の唯一の常設 UI で、そこを 1 アクションの副作用に割り当てると閲覧系が孤立する

OneTab の既定挙動を再現したい場合は、ショートカットのキー割り当てを変えれば済む。

## D03 — マネージャーページ (M3) を作る

決定日: 2026-08-28 / 状態: 採用

popup は 360px 幅で、Recent は 20 件固定。
OneTab の中核操作「グループの中身を見て、要るものだけ取り出して残りは捨てる」を置く場所が無い。

さらに `GET /lists` にはページングもフィルタもソートも無い (`schema.d.ts:229` が `query?: never`) ため、
一覧の絞り込みは必ずクライアント側で行うことになる。その状態を持つ場所としても専用ページが要る。

**`entrypoints/manager/` を新設する。** ただし着手順は M0 → M1 → M2 の後。
土台 (保存の信頼性・サーバー側メタデータ) が無い状態で閲覧 UI を作ると、
表示するデータそのものが信用できない。

popup は「素早く保存する入り口」として残し、閲覧・整理・大量操作をマネージャーページに寄せる。

## D04 — 共有 URL 生成はやらない

決定日: 2026-08-28 / 状態: 採用 (スコープ外に確定)

`docs/karakeep-advanced.md` の Phase 3 に「共有 URL 生成 (Karakeep の public 機能で代替できるか要検証)」がある。
調査した結果:

- `List` に `public: boolean` が実在し (`schema.d.ts:887`)、`PATCH /lists/{listId}` の body でも設定できる (`:1937`)
- **共有 URL を発行・取得する endpoint は 33 パス中に存在しない**

つまりフラグは立てられるが、URL は API の外にある。

**やらない。** 拡張側で Karakeep 本体の URL 規則を組み立てると、
本体のルーティング変更で黙って壊れる。しかも壊れ方が「共有 URL が間違っている」形になり、
共有した相手が 404 を踏むまで気づけない。フラグを立てるだけの機能は本体 UI でできることの劣化コピーなので作らない。

Karakeep 本体が共有 URL を返す API を生やしたら再検討する。

## D05 — LICENSE は MIT

決定日: 2026-08-28 / 状態: 採用

README が "TBD" のまま public repo になっている。

**MIT を採用。** Karakeep 本体との関係で特許条項が必要になる状況が見当たらず、
個人ツールとして利用者に課す制約は最小にしたい。

## D06 — グループのメタデータは `List.description` に「人向け 1 行 + JSON 1 行」で持つ

決定日: 2026-08-28 / 状態: 採用

現状はタブ件数をグループ名の正規表現 (`/\((\d+)\s*tabs?\)\s*$/`) から復元しているため、
Phase 2 で入れた rename 機能を使った瞬間に件数が失われる。

`List.description` がサーバー側に実在し、`GET /lists` で返り `PATCH` で書ける (`schema.d.ts:878` / `:1933`)。

検討した案:

- (a) 1 行 JSON のみ
- (b) 人が読める 1 行のみ + 正規表現でパース
- (c) 人向け 1 行 + JSON 1 行の 2 行

**(c) を採用。**

`description` は Karakeep 本体 UI にもそのまま表示されるので、(a) は本体側に生 JSON が並ぶ。
(b) は結局パースが正規表現に戻り、今と同じ壊れ方をする。

形式:

```
12 tabs · saved 2026-08-28 15:00
<!-- ka:{"v":1,"tabCount":12,"savedAt":"2026-08-28T06:00:00Z","lastOpenedAt":null} -->
```

- パースは `<!-- ka:` で始まる行の JSON だけを見る。人向け行は一切パースしない
- JSON 行が無い / 壊れている場合は `tabCount: null` にフォールバックし、例外を投げない
- 既存グループには JSON 行が無いので、この経路が実際に通る

**chrome.storage に持つ案は採らない。** `chrome.storage.local` は端末間で同期されないため、
別マシンで開いた瞬間にメタデータが空になる。これは Karakeep をバックエンドに選んだ理由と正面から矛盾する。
chrome.storage は引き続き「MRU 表示順のキャッシュ」に限定し、権威データとしては使わない。

`lastOpenedAt` を持たせるのは M4 (auto-archive) の前提。
List に archived フラグは無いので、アーカイブは `parentId` の PATCH で別の親 list に移す方式になる (`schema.d.ts:1935`)。

---

## D07 — Omnibox は姉妹拡張にしない。姉妹は Highlight 1 本

決定日: 2026-08-28 / 状態: 採用

`docs/karakeep-advanced.md` は Highlight と Omnibox を将来の姉妹拡張として挙げているが、
2 つの性質は違う。

分割の根拠は**障害の隔離と要求 permission** に置く。

- Highlight は全ページに content script を挿し `<all_urls>` を実際に要求する。
  そこのバグがタブ保存の経路を巻き込まないことに価値がある → **別拡張**
- Omnibox は新しい permission を要さず、保存済みグループを引くだけ。
  別拡張にすると server URL と API key をもう一度入力させ、host permission をもう一度承認させる
  → **tab-group (またはマネージャーページ) の機能**

規則として固定する: **新しい permission を要さない機能は姉妹拡張にしない。**

したがって「姉妹拡張群」は実際には Highlight 1 本であり、
共有基盤の設計はその 1 本を前提に最小限にする。

## D08 — 共有するのは `packages/karakeep` (schema + client + E2E mock) だけ

決定日: 2026-08-28 / 状態: 採用

D07 で姉妹が 1 本と確定したので、共有面は最小に取る。

**抽出する**

- `src/karakeep/schema.d.ts` と `generate:api` script — 生成物なので重複は純粋な無駄
- `src/karakeep/client.ts` — 認証・リトライ・タイムアウトはどの Karakeep クライアントでも同じ
- `tests/e2e/fixtures/karakeep-mock.ts` — **抽出対象で最も見落とされやすい**。
  343 行あり、2 本目の拡張が E2E を書く時点で必ず必要になる

**抽出しない**

- storage のキー定義。拡張ごとに `chrome.storage` が隔離されるので、キー名を揃えても利益がゼロ
- `flows/` 層。共有に見えて最も危ない。`resolveSubList` の冪等性はジョブの `subListId` と
  結びついており、タブグループ固有の意味を持つ
- messaging の union 型、`<!-- ka: -->` のパーサ、list index キャッシュ

**認証情報は拡張間で共有できない** (別 origin = 別 storage、host permission も別承認)。
`chrome.storage.sync` に逃がす案は採らない — 対象が LAN の私設 IP なので別 LAN のピアには無意味なうえ、
構成が Google に渡る。代わりに options に設定の JSON export / import を置く。
これは拡張間・端末間・再インストールのすべてに効き、storage スキーマを壊さない。

抽出は今やる (純粋な移動で可逆、unit test が回帰を検出できる)。
ただし **Highlight の最初の commit が入る時点で必ず見直す**。それまで package を育てない。

## D09 — グループの順序は `savedAt` に移す。`recentGroupIds` は同点判定に降ろす

決定日: 2026-08-28 / 状態: 採用

現状の順序は `recentGroupIds` (上限 50、`local:`) で、そこから漏れた分は
`GET /lists` が返した行順に委ねている。3 つとも問題がある。

- 上限 50 は週 38 グループ保存する運用で **9 日分**。1 年後には 97% が順序の外
- `local:` なので **2 台目のマシンでは 1 日目から全件が未定義順**
- `listLists` は sort パラメータを持たず (`query?: never`)、応答順序をスキーマが約束していない。
  Karakeep 側のクエリプランが変われば黙って変わる

D06 の `savedAt` がサーバー側にあり、端末に依存しない。これを順序の権威にする。

**移行前のグループには `savedAt` が無い**ので、名前の `YYYY-MM-DD HH:MM` を読むフォールバックを必ず併せる
(リクエスト 0)。フォールバックが無いと、移行前の全グループが未定義順の末尾に落ちる。

`recentGroupIds` は消さず「このマシンで最近触った」の同点判定として残す。

**却下した案**: bookmark の `createdAt` から `savedAt` を逆算する。
重複 URL には既存 bookmark が返る仕様なので、返るのは「グループ内で最も古い bookmark の生成時刻」で、
実際の保存日より系統的に古い。N リクエスト払って狂った値を永続化することになる。

## D10 — サーバーバージョンの変化検知は作らない

決定日: 2026-08-28 / 状態: 採用

**前提の訂正**: 以前「Karakeep に version endpoint は無い」と記録したが、これは誤りだった。
`/api/version` は実在する (`packages/api/index.ts:90`、`/v1` の兄弟)。
生成済みの `/v1` OpenAPI スキーマだけを見て API 全体について否定したのが誤りの原因。

ただし検出機構としては成立しない。

- `docker/Dockerfile:83` が `ARG SERVER_VERSION=nightly`
- release ビルド以外はすべて不変文字列 `"nightly"` を返す
- **nightly / latest を追う self-host ではアップグレードしても値が変わらない。**
  本プロジェクトの運用形態がまさにそれ

「記録した値と違えば警告」は、最も壊れやすい構成で恒久的に沈黙する。
ネガティブコントロールが原理的に取れない検知機構は作らない。

代わりに、**Karakeep を upgrade したら `bun run generate:api` で schema を再生成して diff を見る**、
という手順を README に置く。`openapi-fetch` は実行時検証をしないので、
breaking change は型と実体の乖離として静かに現れる。自動化ではなく手順で対処する領域。
